<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.delta_se.tegalur">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tegalur">
        <activity
            android:name=".ui.activity.DetailOleh"
            android:label="@string/title_activity_detail_oleh"
            android:theme="@style/Theme.Tegalur.NoActionBar"></activity>
        <activity
            android:name=".ui.activity.DetailKuliner"
            android:label="@string/title_activity_detail_kuliner"
            android:theme="@style/Theme.Tegalur.NoActionBar" />
        <activity android:name=".ui.activity.ActivitySplashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.DetailPenginapan"
            android:label="@string/title_activity_detail_penginapan"
            android:theme="@style/Theme.Tegalur.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.activity.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".ui.activity.DetailPariwisata"
            android:label="@string/title_activity_detail_pariwisata"
            android:theme="@style/Theme.Tegalur.NoActionBar" />
        <activity
            android:name=".ui.activity.DetailBerita"
            android:label="@string/title_activity_detail_berita"
            android:theme="@style/Theme.Tegalur.NoActionBar" />
        <activity android:name=".ui.activity.SearchActivity" />
        <activity android:name=".ui.activity.MainActivity" />
    </application>

</manifest>